server_names_hash_bucket_size 128;

server {

    listen 80;  # 👈 Agregado para que Nginx escuche en el puerto 80    
    listen  [::]:80;
	client_max_body_size 100M;
	server_name alcala.governaciondelvalle.gov.co,172.26.159.35;   # ✅ Sin "http://", solo la IP
    
	location / {
        proxy_pass http://alcala-ckan-ckan-1:5000;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto http;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 80;
    }
}

server {

    listen 80;  # 👈 Agregado para que Nginx escuche en el puerto 80    
    listen  [::]:80;
	client_max_body_size 100M;
	server_name roldanillo.governaciondelvalle.gov.co,172.26.159.35;   # ✅ Sin "http://", solo la IP
    
	location / {
        proxy_pass http://roldanillo-ckan-ckan-1:5000;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto http;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 80;
    }
}

server {

    listen 80;  # 👈 Agregado para que Nginx escuche en el puerto 80    
    listen  [::]:80;

    client_max_body_size 100M;	
	server_name harvest.governaciondelvalle.gov.co,172.26.159.35;   # ✅ Sin "http://", solo la IP
    
	location / {
        proxy_pass http://harvest:5000;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto http;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 80;
    }
}


